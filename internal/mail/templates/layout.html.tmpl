<div style="font-family: -apple-system, 'Segoe UI', Roboto, sans-serif; max-width: 480px; margin: 0 auto; padding: 24px; color: #1a1a1a;">
  {{- if .LogoURL}}
  <p style="margin: 0 0 20px;"><img src="{{.LogoURL}}" alt="{{.Project}}" style="max-height: 40px; max-width: 200px;"></p>
  {{- end}}
  <h2 style="font-size: 18px; margin: 0 0 16px;">{{.Subject}}</h2>
  {{- range .Paragraphs}}
  <p style="font-size: 14px; line-height: 1.6; margin: 0 0 12px;">{{.}}</p>
  {{- end}}
  {{- if .ButtonURL}}
  <p style="margin: 24px 0;">
    <a href="{{.ButtonURL}}" style="background: {{.Accent}}; color: {{.OnAccent}}; text-decoration: none; padding: 10px 20px; border-radius: 6px; font-size: 14px; display: inline-block;">{{.ButtonLabel}}</a>
  </p>
  <p style="font-size: 12px; line-height: 1.6; color: #6b6b6b;">Or open this link: <a href="{{.ButtonURL}}" style="color: {{.Accent}};">{{.ButtonURL}}</a></p>
  {{- end}}
  <p style="font-size: 12px; color: #6b6b6b; margin-top: 24px;">— {{.Project}}</p>
</div>
