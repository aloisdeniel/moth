// The SDK version reported in the x-moth-sdk-version header. The placeholder
// below is rewritten by the server when the package is served from /npm, so
// the runtime-reported version always matches the packument (same discipline
// as lib/src/version.dart in the Flutter SDK).
export const mothSdkVersion = '0.0.0'
