# Local development only: moth_billing's pubspec pins
# moth_auth to a hosted /pub source; override it to the sibling source tree
# so `pub get` resolves without a running moth instance.
dependency_overrides:
  moth_auth:
    path: ..
