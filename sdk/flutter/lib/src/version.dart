/// The SDK version reported to the server in `x-moth-sdk-version` metadata.
///
/// Placeholder value: the moth server stamps the real release version into
/// the package tarball it serves at `/pub`, keeping the SDK version in
/// lockstep with the server version.
const String mothSdkVersion = '0.1.0';
